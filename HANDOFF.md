# OnCue — Session Handoff Log
_Last updated: 19 July 2026. Read this first in any new AI session. Companion docs: `WORSHIP_APP_SPEC.md` (v1 chart app), `CHORDS_SPEC.md` (chords system + setlist-first flow)._

## What this is
PWA for the DCC worship team (Dubai, services Thursday + Saturday, timezone **Asia/Dubai** — hardcoded in `page.tsx`, `services/page.tsx`, `library/[id]/page.tsx`). Next.js 16 App Router + Supabase (Postgres/Auth/Realtime/Storage) on Vercel, repo `aaronalm-ops/oncue`. Issac (master role) admins it.

## Current state — all DEPLOYED and WORKING
- **Chart pipeline (v1):** conductor uploads `DAY DD-MM-YYYY CHART.xlsx` → `ingest_chart` RPC (atomic, MERGE semantics). Filename = date. Orange fill `FFFF9900` = intro flag. Parser: `src/lib/parser.ts`, fixtures `Charts Log/`, test `npm run test:parser`.
- **Live Sync** (`services/[id]/live`): shared position via `session_state` realtime, anyone drives, reconnect catch-up refetches on SUBSCRIBED + window online. `updated_by` written on every move.
- **My Part** (`services/[id]/my-part`): per-instrument view, personal notes, "Go Live" song-level driving, arrow-key/pedal nav.
- **Chords system (v2–v6):** bulk PDF upload → **extraction runs in the BROWSER** (`extract-client.ts`; serverless fonts drop text — never parse PDFs server-side) → positioned lines POSTed → pure parser (`src/lib/chords/parse.ts`) → confirm queue (`ChordUploadQueue`) → per-song review editor (`/library/[id]/version/[vid]`) → approve publishes. Internal format: `# Section`, `> Flow x2`, `[G]` inline (`src/lib/chords/format.ts`). Fixtures `chord-samples/` (6 real PDFs; Revelation_Song is a scan → paste-mode), test `npm run test:chords` (includes lossless-render invariant + transpose + reorder + section-map tests).
- **Combined view:** Chart ⟷ Chords panes — swipe w/ wrap-around on phones (pill switcher), side-by-side ≥lg. Live Sync chords follow current section (`ChordsPane`, index-aligned via `mapChartSectionsToChords`). Transpose strip everywhere, per-user key saved in `user_scale_preferences`.
- **Section maps (v5):** manual chart-label → sheet-section overrides, per library song, `chord_section_maps`, set inline in ChordsPane.
- **Setlist-first (v5):** `/services/new` creates a service pre-chart (date + `worship_leader_id` + library songs, links created). Chart upload MERGES by normalized title: song ids/links/notes survive, chart order wins, dropped songs flagged `in_chart=false` (badge on service page, excluded from live views).
- **Open contributions (v6):** ALL members can upload/confirm/edit/approve/rename chords + set maps. Editor-only: chart upload, member admin, service delete, library bulk-delete. Approve = quality gate.
- **Impromptu live share (v7):** library song page → "Share live to today's service" sets `session_state.impromptu_library_song_id`; overlays every Live screen; anyone ends it. Note: uses `.update()` — silent no-op if session_state row missing (QA item #9).
- **PWA:** Samsung Internet blocked by Play Protect (industry-wide) → app detects and routes install to Chrome (`RegisterSW.tsx`).
- **Share Setlist (`ShareSetlist.tsx`, service page):** WhatsApp-formatted message — per-song share-time notes, uniform-key collapse ("All on the Key X"), playlist checkbox + "View playlist ↗", optional LYRICS: bodies lazy-fetched on first tick, per-song section chips (from sheet sections, chords stripped via `lyricsOf`), *bold*/_italic_ WhatsApp markup, char counter + Copy fallback for long messages. Song-key prefill on New Setlist pick = latest reviewed version's stored_key (only fills empty field).

## Database migrations (`supabase/`, run in order, applied through v7; **v8 + v9 written, NOT yet run**)
`schema.sql` → `add_roles` → `add_worship_leader` → `v2_chords_library` → `v3_security_and_ingest` (role-escalation trigger, content RLS, atomic ingest, storage policies) → `v4_chords_phase1` (chord_uploads queue, approve/confirm RPCs) → `v5_setlist_flow` (worship_leader_id, in_chart, chord_section_maps, merge ingest, create_setlist) → `v6_open_chords` → `v7_impromptu` → **`v8_hardening`** (QA #5/6/7/9/11/13 — advisory lock, normalized note-restore, ghost-song cleanup, drop dead `chord_sections`, pinned `handle_new_user` search_path, `set_impromptu` RPC) → **`v9_profiles_teams`** (`profiles.preferred_key` + `teams[]` + `profile_completed_at`, `public_profiles` view) → **`v10_setlist_memory`** (worship-leader song memory: `worship_leader_song_memory` + `library_song_arrangement` tables, `apply_song_memory`/`capture_service_memory`/`search_library` RPCs, `create_setlist` auto-links/creates library songs + restores memory, one-time history backfill) → **`v11_setlist_edit`** (`add_setlist_song` RPC for Edit-Setlist add) → **`v12_search_fix`** (`search_library` collapses whitespace so multi-word lyric phrases match across line breaks) → **`v13_worship_leader`** (`set_worship_leader` RPC — assign/change/clear the leader after creation; leader now optional at create) → **`v14_song_tempo`** (`library_songs.tempo_bpm` + backfill from newest reviewed version bpm — song tempo memory, editable inline from My Part song headers via `TempoChip`, shown for everyone; threaded through `SongChordsData.tempoBpm`; service-chords degrades gracefully pre-migration) → **`v15_fuzzy_merge`** (fuzzystrmatch ext + graduated merge matching inside the service: exact → bracket-stripped (`oncue_strip_brackets`) → levenshtein≤3 → containment≥55%; note restore now keyed by CLAIMED SONG ID not title, so chart renames can't drop notes; returns `ghosts_deleted`) → **`v16_key_change`** (mid-song modulation: `sections.key_change` column; conductor marks it in the Excel section label or comments — "LAST CHORUS (KEY A)", "KEY OF Bb", "(UP 2)"/"(+1)" wrapped-only for relatives; parser (`extractKeyChange` in src/lib/parser.ts) resolves to an absolute key + strips the marker from the label; a marker persists to song end until the next one (`effectiveSectionKeys` in format.ts). Chords render per-section transposed — ChordsPane via `chartKeyChanges` prop, service chords page via `reorderBodyToChart` opts which bakes a relative "· KEY UP n" header cue so user transpose stacks safely. Amber "KEY → A" badges on My Part cards, Live current + next-up, key-journey chips "G → A". Pre-v16 graceful: primary selects include key_change, fallbacks don't. Follow-up idea: ShareSetlist "(Key of G → A)" per-song journey).

**Library upload/review flow (no migration, code-only, Aug 2026):** fixed the stale-list bug (LibraryClient state now syncs with server prop via useEffect + optimistic `onConfirmed` insert from ChordUploadQueue); needs-review songs pinned on top under an amber "NEEDS REVIEW · n" header with "All songs" below; badges say "Needs review" not "0/1 reviewed"; buttons say "Add to library"/"Add all to library"/"Add & paste chords"; scans navigate straight to the version editor after adding. QA follow-ups all done: title edits in an upload card re-run the duplicate matcher live (manual "Adds to" picks are never overridden — `matchTouched`); shared-queue races return 404 ("Already handled by another member") and the card clears with a neutral notice instead of a red error (discard treats 404 as success); Approve honors a sanitized `?returnTo=` (attach + paste flows pass `/services/{id}`, default stays the song page).
**Rule learned the hard way:** run SQL BEFORE deploying code that needs it. **v8 + v9 MUST be run before pushing the current changes** — the new client code selects `preferred_key`/`teams`/`profile_completed_at`/`public_profiles`; if the SQL lags, those profile selects 400 and privileged users transiently lose Upload/Admin (no crash — degrades to member, recovers once SQL is applied).

## Env vars (Vercel)
`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (member deletion + admin emails), `CRON_SECRET` (keepalive cron in `vercel.json`). Buckets: `charts`, `chord-pdfs` (both private).

## Intentional decisions — do NOT "fix"
Viewing another instrument persists as your default (multi-instrumentalists). Anyone drives Live Sync, last-write-wins. Chords fully open to members (v6). Chart tables stay editor-protected. `CHORDS_OPEN_TO_ALL=true` in `src/lib/chords/access.ts`. Chart order always wins on merge. No AI anywhere in the parse/live path — deterministic + human review only. **Teams are a soft tag, NOT a security boundary** (self-pickable; never gate sensitive data on them until/unless hardened to RLS). **`preferred_key` null = show the actual key** (valid "done" state — that's why completion is tracked by `profile_completed_at`, not by a non-null key). **`public_profiles` is intentionally security-definer** (directory of safe columns; do not add `security_invoker`).

## QA BACKLOG (full audit 19 Jul 2026) — ✅ #1–13 ALL DONE (pending v8+v9 deploy). NEXT: verify in prod, then roadmap #2 (Console)
### Stage-critical — ✅ ALL FIXED 19 Jul 2026 (client-only, no SQL; `tsc` + both test suites green)
1. ✅ **My Part "Go Live" never broadcast + instrument saves never fired** — lazy Supabase builders now call `.then()` so they actually execute: `goToSong()` session_state upsert (now also writes `updated_at`) + `handleInstrumentChange` in both `MyPartClient` and `LiveSyncClient`. All fire-and-forget writes log errors on failure.
2. ✅ **Unclamped sync indexes after chart shrink** — both clients clamp on receipt (realtime + refetch). `LiveSyncClient` derives `safeSongIdx`/`safeSectionIdx` for strip/flat-list/next math and `nextFlat` now guards `currentFlatIdx >= 0` (killed the "jump to song 0"). `MyPartClient` `activeSong` guarded `?? songs[0]`.
3. ✅ **ChordsPane follow-scroll hijacked horizontal swipe** — replaced `scrollIntoView` with manual `scrollTop` on the nearest `.overflow-y-auto` pane only, so the chart⟷chords snap container no longer gets panned.
4. ✅ **Realtime resubscribe flaws** — both clients: single retry timer (guarded, no channel stacking), superseded-channel callbacks ignored via an `active` flag, `CLOSED` now retries. `MyPartClient` go-live channel gained a `.subscribe(status)` callback + a real `liveStatus` badge (LIVE/SYNC/OFFLINE) instead of a badge that always claimed LIVE.
### Data integrity — ✅ ALL FIXED 19 Jul 2026 (v8 SQL + client)
5. ✅ `confirm_chord_upload` double-confirm → `pg_advisory_xact_lock(hashtext('confirm_chord_upload:'||id))` before the SELECT; existing not-found recheck makes the 2nd call raise cleanly.
6. ✅ Note restore now normalizes both sides — `_note_snapshot` stores `oncue_norm_title`/`oncue_norm_label`; restore join + inner NOT EXISTS compare normalized, matching the merge. Corrected-title re-uploads keep their notes.
7. ✅ Ghost cleanup in `ingest_chart`: unclaimed songs with no `song_links` AND no `user_notes` are DELETED (cascades); ones carrying either are flagged `in_chart=false` as before.
8. ✅ `transposeBody` gates on `isChordToken` — `[Build]`/`[Instrumental]`/`[Bridge]` survive; fixture added to `test:chords`.
9. ✅ Share-live/end now call `set_impromptu` RPC (atomic upsert touching only impromptu cols; never resets live position; creates the row if absent). Both `SongDetailClient.toggleShareLive` + `LiveSyncClient.endImpromptu`.
10. ✅ ONE resolver: `fetchServiceChords` in `service-chords.ts` is the single source of truth; the service page now calls it (was a divergent copy). `normTitle` exported + reused by the standalone chords page.
11. ✅ `approve_song_version` no longer writes `chord_sections` (ignores `p_sections`); table DROPPED. Viewer re-parses `content_chordpro`. Injection surface gone.
12. ✅ `fetchServiceChords` picks the reviewed version whose `stored_key` matches the chart scale (by pitch class), else newest.
13. ✅ single-version DELETE now removes the PDF; `songs` PUT scoped `.eq('service_id')` (+ id-belongs-to-service filter); keepalive fails CLOSED when `CRON_SECRET` unset; upload route caps 10 MB; share-live button uses try/finally.
### Clean (audited, no action)
RLS cumulative state all tables/buckets; IDOR (notes/prefs `WITH CHECK` correct); role escalation blocked by trigger; jsonb RPCs injection-safe; XSS (React escaping); admin routes gated before service-role use.

## Features added 19 Jul 2026 (v9 SQL + client) — pending v8+v9 deploy
- **Preferred scale + instrument-aware transpose (Option A, agreed w/ Issac):** `profiles.preferred_key` (null = actual/no transpose). Chords re-letter into the preferred key; the instrument decides the *hint* — keyboard shows a transpose number (E song, F pref → −1), guitar shows a **capo** position, everyone else / "none" sees the actual key. Logic in `format.ts` (`instrumentTransposeMode`, `transposeHint`, capo-vs-keyboard family regex). Threaded into `ChordsPane` + `ChordSheetViewer` via new `instrument`/`preferredKey`/`actualKey` props; target-key precedence = per-song strip → global preferred → chart scale → sheet key. Per-song strip still overrides. Hint hidden when value 0. Fixtures in `test:chords`.
- **Teams (soft tags, not a security boundary — agreed; may tighten to hard RLS later):** `profiles.teams[]` ∈ {`worship`,`sound`,`media`} (CHECK-constrained; existing members backfilled to `worship`). SEPARATE from `role` (permissions) and from the per-service `worship_leader_id`. Self-picked at signup, admin-editable. Admin panel has tap-to-toggle team chips (`/api/admin/members` PATCH gained a `teams` branch). **No view filtering yet** — that lands with the Console/Lyrics views.
- **Profile completion:** onboarding screen (`auth/select-instrument`) now also captures preferred key + teams and stamps `profile_completed_at`. Existing members get a one-time dismissible `ProfileCompletionModal` on `/services` (shown when `profile_completed_at IS NULL`; "Later" = sessionStorage; saving stamps the flag so "Actual"/no-key still counts as done → never nags again).
- **Initials avatars = the "dp":** shared `Avatar` component (initials). Worship leader shown on the service page via tappable `LeaderBadge` (→ name/instrument/teams card) and on each services-list card; the logged-in user's own avatar is in `UserMenu`. Reads names via the `public_profiles` view (members can't read other `profiles` rows directly — RLS). **`public_profiles` is intentionally security-definer; do not "fix" the advisor warning** (see v9 SQL comment).

## Setlist memory + smarter entry (v10) — BACKEND DONE, UI PHASE PENDING
Agreed w/ Issac: when a leader builds a setlist, repeating songs pre-fill last time's flow + conductor notes (`sections.comments` + per-instrument `instructions`) + YouTube (`reference_links`); Wednesday's Excel still overwrites. Decisions: **fall back to any leader** if this leader never led it; **library-linked only** (every setlist song is now auto-linked/created, so this covers all); pre-fill on **create AND edit-add**.
- **Done (v10 SQL + upload route):** TWO tables (both SELECT-only RLS, writes via SECURITY DEFINER) — `worship_leader_song_memory` (per-leader) + `library_song_arrangement` (song's last-known arrangement = fallback + history seed). `_service_song_snapshots` helper resolves each song by **link OR normalized-title** and snapshots it (DISTINCT ON per library song = reprise-safe). `apply_song_memory` (leader memory → last-known fallback; fills BLANK songs only; editor-gated). `capture_service_memory` (upsert both, date-guarded; called by upload route after ingest). `create_setlist` v2 (auto-resolve/create+link + restore + instruments union). `search_library` (title + de-chorded lyrics). **One-time backfill** at the end of v10 replays every existing service oldest→newest, so the ~5–6 months of history seed memory on day one (a song seeds if it resolves to a library entry by link or title). Reviewed clean.
- **UI phase — ✅ DONE (v11 SQL + client):** `add_setlist_song` RPC (`v11_setlist_edit.sql`) for the edit path (same resolve/link/memory, blank-only). `search_library`-backed `SongPicker` component (`/api/library/search` route) with lyric snippet preview, `↩ saved` hint (arrangement exists), has-chords flag, and a "create new — needs chords" path; wired into `NewSetlistClient` and `EditSetlistClient` (new songs POST through the songs route → `add_setlist_song`, then take submitted order). **"Needs chords"** amber badge in library list, builder, service page. **YouTube playlist** button on the service page (`src/lib/youtube.ts` → anonymous `watch_videos` queue, "N of M have videos", hidden if none). Provenance is the `↩ saved` chip in the picker (fuller "from <name>" deferred). `tsc` + both suites green.

## PERF + WORKFLOW QA (2nd full audit, 19 Jul 2026 — two-agent pass)
### Root cause of "3–4s per tap" (diagnosed, partly fixed)
Every page = fully dynamic SSR with 5–11 SEQUENTIAL network round-trips (middleware `getUser()` + page `getUser()` + serial DB queries), most routes had NO loading.tsx → tap shows nothing until the whole chain finishes. Likely multiplied by Vercel↔Supabase region mismatch (owner must check).
### Shipped this round ✅
loading.tsx on all 9 missing routes (instant spinner on every tap); `/services/[id]` queries parallelized (9→~4 stages); `fetchServiceChords` gained `{light:true}` (service-page badges no longer download every chord body; bodies also now filtered `content_chordpro IS NOT NULL`); 4 `push()+refresh()` double-renders removed (NewSetlist×2, EditSetlist, VersionEditor); **Live Service card restored on the service page (Live Sync had NO link anywhere — lost in commit 2d146d8!)**; My Part/Live strip back-button now returns to the service hub not /services; "Song chords" section shows even when zero songs have chords (the one-tap add-chords rows are most needed then).
### OWNER INFRA CHECKLIST — status 18 Sep 2026: JWT asymmetric keys ✅ (ECC P-256, since ~Jun), region match ✅ (Supabase ap-northeast-1 Tokyo, Vercel moved to hnd1), Fluid Compute ✅, Supabase Pro ✗ (declined — Free tier + keepalive cron stays)
P8 ✅ (18 Sep 2026) v20_service_bundle.sql: `service_bundle(p_service_id, p_light)` SECURITY INVOKER jsonb RPC → hub 5 sequential hops→1, Stage View 4→1, Live 5→1. `src/lib/service-bundle.ts` (`fetchServiceBundle`) parses it and falls back to the legacy multi-query path if the RPC errors (deploy code before SQL = slower, never broken). Chord resolution split into pure `resolveServiceChords()` in service-chords.ts (shared by both paths; duplicate library titles now resolve to the OLDEST row = what ingest picks). `/` launch redirect and `/services` list parallelised. Remaining hop on every page is proxy.ts getClaims (local, ~0ms). NOT done, deliberately: role-in-JWT hook (bundle carries role for free; hook would add a dashboard step + 1h stale-role semantics for ~10ms on admin-only pages) and `prefetch={true}` on the Stage View hero (full dynamic prefetch is cached 5 min by staleTimes.static → stale notes on re-entry unless every write calls router.refresh()).
1. Vercel Settings→Functions region vs Supabase Settings→Infrastructure region — MUST match. For Dubai users ideal: Supabase ap-south-1 (Mumbai) + Vercel bom1, or eu-central-1+fra1. Mismatch = 100–250ms × every serial query.
2. Vercel plan cold starts (Observability→Functions p95; enable Fluid Compute). 3. Supabase Free tier→Pro (keepalive cron implies Free = slow shared compute). 4. Index advisor: songs.service_id, sections.song_id, instructions.section_id, song_links.song_id, song_versions(library_song_id,reviewed_at), services.service_date; RLS policies should use `(select auth.uid())` not bare `auth.uid()`.
### Perf backlog — ✅ ALL DONE (Aug 2026 sprint)
P1 ✅ getClaims local JWT validation in proxy.ts + getAuthUser helper across all 22 server files (safe pre-key-migration: supabase-js falls back to network on HS256; Issac must enable asymmetric signing keys in the dashboard for the win to land). P2 ✅ my-part 6→3 stages, live 9→3, library 4→2, library/[id] parallelized too. P3 ✅ SongDetailClient local state (rename/delete, lazy-body-preserving prop sync), queue + bulkDelete refreshes dropped; WorshipLeaderPicker refresh KEPT deliberately (server badge correctness). P4 ✅ songs PUT + adopt links batched/concurrent. P5 ✅ v17_perf.sql: oncue_norm_title_full (matches JS normTitle exactly — old oncue_norm_title doesn't collapse whitespace!) + generated norm_title column + index pack; resolver filters .in(norm_title) with straggler fetch for renamed links + pre-v17 full-scan fallback. P6 ✅ sw.js v8: navigations network-first with 2.5s patience → cached copy, background refresh. P7 ✅ chord_uploads explicit columns, lazy version bodies on /library/[id] (spinner on expand), services .limit(50); start_url kept "/" deliberately (P1 made the smart redirect cheap; service-day landing is a feature).

### Old perf backlog reference (superseded)
P1 middleware: replace `getUser()` with `getClaims()` local JWT validation (needs asymmetric keys enabled in Supabase) + same in pages — saves 2 auth round-trips per tap. P2 parallelize my-part (10→4) + live (9→4) + library pages. P3 replace remaining `router.refresh()` with local state (WorshipLeaderPicker, SongDetailClient×2, ProfileCompletionModal, ChordUploadQueue, LibraryClient redundant one). P4 batch N+1 writes (songs PUT loop → single upsert; NewSetlistClient link loop → one upsert array). P5 fetchServiceChords: filtered library lookup (norm_title column) instead of full-table scan; limit versions/song. P6 SW: stale-while-revalidate nav with 2s timeout. P7 trim payloads (chord_uploads select w/o draft_body; lazy version bodies on /library/[id]; .limit(50) services; PWA start_url → /services to skip the / redirect double-chain).
### Workflow dead-ends — ✅ ALL DONE (Aug 2026 sprint)
W2 ✅ impromptu mirrored in My Part live mode (applyImpromptu + already-live catch-up fetch + overlay + end button; clears on leaving live). W6 ✅ upload success shows "Chart merged (n songs) — view service →". W7 ✅ list-page WhatsApp share: neutral wording + direct service URL. W8 ✅ Edit Setlist back arrow confirms when dirty. W9 ✅ ChordsPane empty state = attach deep-link CTA (My Part + Live). W10 ✅ "Auto" chip DELETES the key pref (old reset saved sheet key as pref); pinned state drives highlight. W11 ✅ profile save failures surfaced (select-instrument + ProfileCompletionModal). W12 ✅ New Setlist leader defaults to creator-if-leader else Unassigned. W13 ✅ service delete surfaces errors, no false success. W15 ✅ download failures are styled HTML pages with back link. W16 ✅ role-neutral empty copy ("an admin can delete and re-upload"). W17 ✅ chords/[songId] empty state = attach CTA. W18 ✅ saveMapping reverts optimistic state + shows error on failure.

### Old workflow backlog reference (superseded)
W2 impromptu share renders ONLY in Live view — mirror overlay in My Part live mode. W4 ✅ DONE (Aug 2026: returnTo threaded confirm→editor→approve; editor back honors it too). W6 chart upload success = silent refresh — add "Chart merged → view service" link. W7 WhatsApp share: only on list page, chart-worded even for drafts, no URL — move to service page + include link. W8 Edit Setlist back-arrow discards unsaved changes w/o confirm (has `dirty`, unused). W9 ChordsPane "No chords linked" empty state → make it the attachSong deep-link (same as service rows). W10 key preference sticky forever — add "Auto (chart)" chip that DELETEs the pref row (reset currently SAVES sheet key as pref!). W11 profile-save failures silent (select-instrument:67, ProfileCompletionModal:58) — surface error. W12 New Setlist silently defaults leader to first worship_leader alphabetically — default admins to Unassigned. W13 service delete shows false success on error (ServicesClient handleDelete ignores error). W14 ✅ DONE (Aug 2026: scan confirm always pushes to the version editor). W15 download 404 JSON → styled page. W16 "No songs found" tells members to re-upload (can't) — role-aware copy. W17 chords/[songId] empty state → attachSong CTA. W18 saveMapping optimistic w/o revert on failure.

## IDENTIFY ("which song is this?") — v21, 18 Sep 2026
`/identify` (+ `?from=` back link). Phone listens via Web Speech API (no API key, nothing uploaded); rolling last-10-words → `identify_song(p_text)` (pg_trgm `word_similarity` against a STORED `song_versions.lyrics_norm` generated column + title match; SECURITY INVOKER so members only match reviewed sheets). Client keeps best-score-per-song across calls. Key detection is ON-DEVICE (`src/lib/audio/key-detect.ts`: FFT → peak-only chroma → Krumhansl-Kessler; `snapToMode` uses the identified sheet's major/minor to kill the relative-key coin-flip). Auto-stops at 30s or when match ≥0.85 + key confident after 8s. Confirm sheet → `set_impromptu(service, song, key)` — the FIRST caller that ever passes a non-null key. Live: `sharedKey` now beats the personal pref (was the reverse; every share was null before so nothing regresses), and a key-only change re-keys without refetch. Typed fallback for iOS (SpeechRecognition support in installed iOS PWAs is unverified — TEST ON A REAL IPHONE). FIELD TEST #1 (Issac, Android): key right once then wrong, words NEVER matched. Two causes, both fixed: (1) `identify_song` had `SET search_path = public` — on Supabase pg_trgm lives in `extensions`, so `word_similarity` was 'does not exist' on every call (reproduced locally by moving the extension); now `public, extensions`. (2) getUserMedia + SpeechRecognition concurrently starved the recogniser on Android. Now SEQUENTIAL: mic → key detection for 4.5s → released → voice. Chroma is per-frame-normalised + log-compressed (one held note no longer decides the key), fundamentals only (70–1200 Hz); snapToMode prefers the RELATIVE key. UI shows 'N words heard · M lookups · voice:<state> · key C .81 / G .74 / …' and every RPC error in amber — never console-only again.
FIELD TEST #2: DB fixed, but the browser recogniser mangles SINGING ('Jesus name above all names' → 'beautiful beautiful say if you'); it's a dictation engine. Added Whisper: `POST /api/identify/transcribe` — provider by env, in order: NVIDIA_API_KEY → hosted whisper-large-v3 on build.nvidia.com via Riva gRPC (`src/lib/riva/asr.ts`, protos vendored in `src/lib/riva/proto`, `outputFileTracingIncludes` in next.config so Vercel ships them; hosted NVIDIA ASR is gRPC-ONLY, function-id b702f636-… for whisper, d8dd4e9b-… for parakeet); GROQ_API_KEY → whisper-large-v3-turbo HTTPS; OPENAI_API_KEY → whisper-1; none → 501. Client records RAW PCM via ScriptProcessor → 16 kHz mono WAV per 5 s clip (`src/lib/audio/wav.ts`) — every provider takes WAV; MediaRecorder was dropped (codec differs per phone, NVIDIA won't take webm). gRPC path could not be exercised from the sandbox (proxy 403s grpc.nvcf.nvidia.com) — proto load + client construction verified, first real call happens on Vercel. Client `stt='server'` mode: ONE getUserMedia stream for the whole listen — key analyser runs continuously, MediaRecorder cuts 5s clips → Whisper → transcript → identify_song. No SpeechRecognition in that mode at all. `stt='browser'` (no key) keeps the sequential key→words path. Audio is forwarded and discarded, never stored. FIELD TEST #3 (NVIDIA Whisper live): words + song now right; key still wrong for a lone voice ('Blessing and Honour' in A → C#: melody on the 3rd). Fix = SONG-AWARE key (`src/lib/audio/song-key.ts`): once the song is identified the client fetches its reviewed sheet, builds a 12-bin note profile from the [chords] (root 1.5 / 3rd 1 / 5th .8 / extensions / slash bass), rotates it through 12 transpositions and scores each = pearson(sqrt chroma, sqrt profile) − 1.5 × share of chroma energy on notes the song never uses. That out-of-scale penalty is what a single voice actually gives you. Synthetic tests: every case that fooled KK (mediant-heavy melody, 5th/3rd hovering, transposed to C) resolves correctly. Generic KK remains the fallback when the song has no sheet. Chip says 'fitted to the song’s chords'; diagnostics prints `key(song) A 0.61 / D 0.49 / …`. Entry points: hub card, Live instrument row chip. Live target (`src/lib/live-target.ts`): today's service, else next upcoming, else last — replaces the today-only rule on the library share button too.
PWA icon: `src/app/icon-*.png/route.tsx` (old ▶ logo, shadowed by public/ PNGs) deleted; sw.js v9 serves manifest + icons network-first (cache-first made Chrome's icon-hash check see the old bytes forever → WebAPK never updated icon/splash).

## BOTTOM NAV + ME (v22, 18 Sep 2026)
Tabs are level; the ACTIVE tab lifts its icon into a purple disc (CSS transition). Nothing is permanently raised — Issac explicitly rejected a permanent Listen button ("not used often enough to earn a highlight").
`src/components/BottomNav.tsx` — Home · Chords · [Listen] · Me, mounted in the root layout; `SHOW_ON` allow-list = browsing pages only (services list, hub, library, song page, stats, admin, me, identify). NEVER on Stage View / Live / chord sheet / version editor — their bottom edge is Prev/Next. Root layout runs one tiny `services.eq(service_date, today)` query (parallel with the page) for the Home dot + direct landing on today's hub. `/me` replaces the avatar dropdown (UserMenu.tsx deleted): identity card, Edit profile → /auth/select-instrument, Stats (leaders+), Admin (privileged), Sign out. Home header is now wordmark + Upload only. Nav pages use `pb-32`; anything fixed-bottom on a nav page (identify confirm sheet) offsets by `BOTTOM_NAV_HEIGHT` + safe-area.

## ROADMAP (agreed direction, by persona)
1. **Hardening pass** = QA items 1–13 ✅ DONE (19 Jul). Client fixes + `v8_hardening.sql`. Just needs v8+v9 run in Supabase, then push.
2. **Sound-team Console view** (Issac's ask): landscape FOH view riding Live Sync — current section huge; per-instrument prominence derived from chart cells (non-SILENT = active, intro flag = lead-in) with silent instruments dimmed; next section preview for pre-fade; key/BPM; worship leader name. Optional precision: per-section "lead" marker settable in Edit Setlist (small col on `sections` or JSON on service). Route suggestion: `/services/[id]/console`.
3. **Projection/lyrics view** (cheap, high value): chord bodies already contain lyrics — strip chords, render huge on black, follow live section. `/services/[id]/lyrics`.
4. **Worship director analytics:** song rotation frequency, key history per leader, planning calendar, CCLI usage report (ccli_number already captured).
5. **Musicians:** true offline bundle of active service (last unmet v1 spec promise — localStorage/IndexedDB snapshot + hydrate on fetch fail); per-user chord annotations; document pedal support (arrow keys already work in My Part live mode).
6. **Platform later:** provenance columns (uploaded_by/approved_by on versions — matters now contributions are open), multi-team tenancy (team_id + invites), audit trail.

## Working conventions for this repo
- Tests: `npm run test:parser` and `npm run test:chords` — run BOTH before any push touching parsing/format/merge. Fixtures are sacred; new bug = new fixture first.
- Every broken real-world file (chart or PDF) gets added to `Charts Log/` or `chord-samples/` with expectations in the test scripts.
- Verify with `npx tsc --noEmit` before handing anything to Issac.
- Deploy = run new SQL in Supabase editor FIRST, then `git add -A && git commit && git push` (Vercel auto-deploys). Issac runs these himself — always give him the exact commands.
- Two stray duplicate PDFs in `chord-samples/` (`-2f2f2258`, `-446248bb` suffixes) are junk; deletable.
- Keep answers concise; Issac prefers direct, step-by-step instructions for anything operational.
